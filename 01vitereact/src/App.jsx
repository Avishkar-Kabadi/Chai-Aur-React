import Chai from "./chai";

function App() {
  return (
    <>
      {/* In the provided code snippet, `<Chai />` is a JSX element that is being used as a component in
    the `App` function. When `<Chai />` is included within the `return` statement of the `App`
    function, it will render the `Chai` component in the React application's UI. This syntax is
    specific to React and is used to include and render components within other components.  */}
      <Chai />
      <h1>Chai</h1>
      <p>Hello Chai</p>
    </>
  );
}

export default App;
